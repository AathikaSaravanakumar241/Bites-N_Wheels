spring.application.name=bitesonwheels
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.default_schema=public
jwt.expiration-ms=86400000
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.ulggwujfsibhvaguotih
spring.datasource.password=Aathika&@241
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=40000
spring.datasource.hikari.connection-test-query=SELECT 1

# The Supabase pooler is in ap-northeast-1 (Tokyo); the TLS handshake from here
# regularly takes 7s+ and blew past pgjdbc's 10s default, failing startup.
spring.datasource.hikari.data-source-properties.connectTimeout=30
spring.datasource.hikari.data-source-properties.socketTimeout=60
spring.datasource.hikari.data-source-properties.tcpKeepAlive=true

# Demo mode: narrows the customer area picker (and the owner's stop picker)
# to these areas, so every area a customer can choose actually has food.
# Clear this line to serve all 50 stations again. See docs/demo-4-areas.sql.
app.demo.areas=Adyar,Anna Nagar,T Nagar,Velachery
